<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!--
        extractNativeLibs 必须为 true。
        YouTube 引擎把 CPython 运行时打包成 lib/<abi>/libpython.zip.so ——
        之所以叫 .so，是因为 Android 只会把匹配 lib/<abi>/*.so 的文件解压到
        应用的原生库目录，库正是靠这个约定把解释器「夹带」进去的。
        若设为 false，安装时不解压，Python 就找不到自己的运行时。
        （该属性默认值本就是 true，这里显式写出来是防止将来被误改。）
    -->
    <!--
        usesCleartextTraffic 保持 false，只给回环地址开例外。
        例外由 networkSecurityConfig 提供 —— YouTube 的预览转发监听
        http://127.0.0.1，那是明文流量；不放行的话 MediaPlayer 会直接报
        source returned error -1，而转发侧连连接都收不到。
        （API 24 起，存在 networkSecurityConfig 时该属性本身被忽略，
         这里保留是为了对更低版本也表达同一个意图。）
    -->
    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.BiliGrab">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 支持从 B 站 App「分享」直接唤起 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!--
            应用内登录。不导出：它只能由本应用拉起，
            外部的 ACTION_VIEW 不该把用户丢进一个登录页。
        -->
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:label="@string/login_title"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize" />

        <!--
            下载管理页。不导出：它读的是本应用的下载记录，
            外部应用没有任何理由直接打开它。
            顶栏会自己避开状态栏，所以保留 configChanges 让转屏不重建 ——
            重建会丢掉正在滚动的列表位置。
        -->
        <activity
            android:name=".DownloadsActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:label="@string/downloads_title"
            android:launchMode="singleTop" />

        <service
            android:name=".DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
